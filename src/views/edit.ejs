<% include partials/header %>


    <div class="container">
        <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-body">
                        <form action="/update/<%= tarea._id%>" method="POST">
                            <div class="form-group">
                                <input type="text" name="titulo" placeholder="Introduzca un título de tarea" class="form-control" value="<%= tarea.titulo %>">
                            </div>
                            <div class="form-group">
                                <textarea name="descripcion" cols="80" placeholder="Introduzca la descripción de la tarea." class="form-control"><%= tarea.descripcion %></textarea>
                            </div>
                            <button type="submit" class="btn btn-primary btn-block"> 
                                Modificar 
                            </button>

                        </form>
                    </div>
                </div>

            </div>
            <div class="col-md-4"></div>
        </div>
    </div>

<% include partials/footer %>
